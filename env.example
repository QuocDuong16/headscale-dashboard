# Headscale API URL
# This is used by Next.js API routes to proxy requests to Headscale backend
# Also used in setup guide examples (exposed via /api/config endpoint)
# Your Headscale server URL
HEADSCALE_API_URL=https://your-headscale-server.com

# Note: NEXT_PUBLIC_API_BASE_URL is no longer needed
# The frontend now uses /api/proxy which is handled by Next.js API routes

